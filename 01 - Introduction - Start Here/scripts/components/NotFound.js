/*
Not found
*/

// when you don't specify path, it defaults to node_modules/
import React from 'react';

var NotFound = React.createClass({
    render : function(){
        return (
            <h1>Not Found!</h1>
        )
    }
});

export default NotFound;
